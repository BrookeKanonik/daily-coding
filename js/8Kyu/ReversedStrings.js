//Instructions:

/*
Complete the solution so that it reverses the string passed into it.

'world'  =>  'dlrow'
'word'   =>  'drow'

* @param {string} word - the word we will reverse
* @return {string} word reversed - the word we reversed
*/

//Example: 'happiness'.split('').reverse().join('') ===> 'ssenippah'

//Initial code (works) and best practice:

const solution = str => str.split('').reverse().join('')